#summary 静态语义检查

静态语义检查分为下面几个方面:
  # 名字检查
    * 类型名
    * 函数名
    * 变量名
  # 类型匹配检查
    * 各种运算 (比如算术,逻辑)的类型约束
    * 函数调用实参和形参的类型匹配
    * 函数实际返回类型和声明的返回类型是否匹配
  # 变量初始化检查 (非必须)
    * 检查局部变量在引用前是否已经初始化


对于OOC(L2)语言的静态语义检查需要解决的几个问题:
  # 作用域问题
